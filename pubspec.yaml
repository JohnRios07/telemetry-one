name: telemetry_one
description: "Telemetry One - Real-time sim racing telemetry dashboard"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.14.0-10.0.dev

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  pointycastle: ^4.0.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  shared_preferences: ^2.3.4
  wakelock_plus: ^1.2.10
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
